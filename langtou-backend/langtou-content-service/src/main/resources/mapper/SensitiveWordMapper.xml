<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langtou.content.mapper.SensitiveWordMapper">

    <!-- 批量查询敏感词 -->
    <select id="selectBatchByIds" resultType="com.langtou.content.entity.SensitiveWord">
        SELECT * FROM sensitive_word WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 分页查询敏感词列表（支持条件筛选） -->
    <select id="selectPageByCondition" resultType="com.langtou.content.entity.SensitiveWord">
        SELECT * FROM sensitive_word
        <where>
            <if test="word != null and word != ''">
                AND word LIKE CONCAT('%', #{word}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="source != null and source != ''">
                AND source = #{source}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
